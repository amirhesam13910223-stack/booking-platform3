import{useEffect}from"react";import{supabase}from"@/lib/supabaseClient";import{useNavigate}from"react-router-dom";export default function GoogleCallback(){const nav=useNavigate();useEffect(()=>{supabase.auth.onAuthStateChange((e,s)=>{if(e==="SIGNED_IN"&&s)nav("/dashboard");});},[nav]);return(<div className="flex items-center justify-center min-h-screen bg-surface-950"><p className="text-surface-400">Redirecting...</p></div>);}
